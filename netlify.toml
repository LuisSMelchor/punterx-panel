[build]
  command = "npm run build"
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["openai", "node-fetch"]

[functions."autopick-vip-nuevo"]
  schedule = "*/15 * * * *"

[functions."verificador-aciertos"]
  schedule = "0 * * * *"

[functions."analisis-semanal"]
  schedule = "0 10 * * 1" # Cada lunes a las 10:00am (hora del servidor)

[functions."diagnostico-total"]
  name = 
  schedule = "*/10 * * * *"   # refresco del dashboard
