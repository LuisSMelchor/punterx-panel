[build]
  publish = "."

[functions]
  directory = "netlify/functions"

[dev]
  framework = "#static"
  port = 8888
  autoLaunch = false
  publish = "."

[functions]
directory = "netlify/functions"
included_files = ["netlify/functions/_lib/**", "_lib/**"]
