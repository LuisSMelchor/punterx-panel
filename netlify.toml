[build]
  command = "npm run build || echo 'no build step'"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/window"
  to   = "/.netlify/functions/window-log"
  status = 200
  force = true
